# Backend API URL — change if your FastAPI runs elsewhere
NEXT_PUBLIC_API_URL=http://localhost:8000

# Set to "true" to bypass the backend and use in-memory mocks (legacy demo)
NEXT_PUBLIC_MOCK_MODE=false
